<!DOCTYPE html><html lang="zh-CN"><head>    <meta charset="UTF-8">    <meta name="viewport" content="width=device-width, initial-scale=1.0">    <title>至尊打砖块 - 排行榜版</title>    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-database-compat.js"></script>    <style>        * {            margin: 0;            padding: 0;            box-sizing: border-box;        }        body {            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;            background: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 100%);            color: #ffffff;            min-height: 100vh;            display: flex;            justify-content: center;            align-items: center;            padding: 20px;        }        /* 登录界面样式 */        #loginScreen {            background: rgba(255, 255, 255, 0.1);            backdrop-filter: blur(10px);            border-radius: 20px;            padding: 40px;            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);            border: 1px solid rgba(255, 255, 255, 0.2);            text-align: center;            width: 100%;            max-width: 400px;        }        #loginScreen h1 {            font-size: 2.5rem;            margin-bottom: 10px;            background: linear-gradient(45deg, #667eea 0%, #764ba2 100%);            -webkit-background-clip: text;            -webkit-text-fill-color: transparent;            background-clip: text;        }        #loginScreen p {            color: rgba(255, 255, 255, 0.7);            margin-bottom: 30px;        }        #usernameInput {            width: 100%;            padding: 15px;            margin-bottom: 20px;            border: 1px solid rgba(255, 255, 255, 0.2);            border-radius: 12px;            background: rgba(255, 255, 255, 0.1);            color: #ffffff;            font-size: 1rem;            outline: none;            transition: all 0.3s ease;        }        #usernameInput:focus {            border-color: #667eea;            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.2);        }        #loginBtn {            width: 100%;            padding: 15px;            background: linear-gradient(45deg, #667eea 0%, #764ba2 100%);            color: white;            border: none;            border-radius: 12px;            font-size: 1.1rem;            font-weight: 600;            cursor: pointer;            transition: transform 0.2s ease, box-shadow 0.2s ease;        }        #loginBtn:hover {            transform: translateY(-2px);            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);        }        #loginBtn:active {            transform: translateY(0);        }        /* 游戏界面样式 */        #gameScreen {            display: none;            flex-direction: column;            align-items: center;            width: 100%;            max-width: 900px;        }        .game-header {            display: flex;            justify-content: space-between;            align-items: center;            width: 100%;            margin-bottom: 20px;            padding: 15px 20px;            background: rgba(255, 255, 255, 0.1);            backdrop-filter: blur(10px);            border-radius: 15px;            border: 1px solid rgba(255, 255, 255, 0.2);        }        .header-info {            display: flex;            gap: 40px;            font-size: 1.2rem;            font-weight: 600;        }        .score-display, .lives-display {            display: flex;            align-items: center;            gap: 8px;        }        .score-display span, .lives-display span {            color: #667eea;            font-size: 1.4rem;        }        /* 排行榜样式 */        #leaderboard {            background: rgba(255, 255, 255, 0.1);            backdrop-filter: blur(10px);            border-radius: 15px;            padding: 20px;            border: 1px solid rgba(255, 255, 255, 0.2);            width: 100%;            margin-bottom: 20px;        }        #leaderboard h2 {            font-size: 1.3rem;            margin-bottom: 15px;            color: #667eea;            text-align: center;        }        #leaderboardList {            list-style: none;            display: grid;            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));            gap: 10px;        }        .leaderboard-item {            background: rgba(255, 255, 255, 0.05);            padding: 10px 15px;            border-radius: 8px;            display: flex;            justify-content: space-between;            align-items: center;            border-left: 3px solid #667eea;        }        .rank-number {            font-weight: 700;            color: #667eea;            min-width: 25px;        }        .player-name {            flex: 1;            margin: 0 10px;            overflow: hidden;            text-overflow: ellipsis;            white-space: nowrap;        }        .player-score {            font-weight: 600;            color: #4ade80;        }        /* 游戏控制区域 */        #startBtn {            padding: 12px 40px;            font-size: 1.1rem;            font-weight: 600;            cursor: pointer;            background: linear-gradient(45deg, #667eea 0%, #764ba2 100%);            color: white;            border: none;            border-radius: 12px;            margin-bottom: 20px;            transition: transform 0.2s ease, box-shadow 0.2s ease;        }        #startBtn:hover {            transform: translateY(-2px);            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);        }        #gameCanvas {            border: 2px solid rgba(102, 126, 234, 0.5);            border-radius: 15px;            background: #000000;            box-shadow: 0 8px 32px rgba(102, 126, 234, 0.2);        }        .game-tip {            margin-top: 15px;            font-size: 0.9rem;            opacity: 0.7;            text-align: center;        }        /* 响应式设计 */        @media (max-width: 768px) {            .header-info {                gap: 20px;                font-size: 1rem;            }            #gameCanvas {                width: 100%;                height: auto;            }            #leaderboardList {                grid-template-columns: 1fr;            }        }    </style></head><body>    <!-- 登录界面 -->    <div id="loginScreen">        <h1>🧱 至尊打砖块</h1>        <p>输入您的游戏昵称开始挑战</p>        <input type="text" id="usernameInput" placeholder="请输入您的昵称" maxlength="15">        <button id="loginBtn">开始游戏</button>    </div>    <!-- 游戏界面 -->    <div id="gameScreen">        <div class="game-header">            <div class="header-info">                <div class="score-display">得分: <span id="score">0</span></div>                <div class="lives-display">生命: <span id="lives">3</span></div>            </div>            <div style="color: rgba(255,255,255,0.7); font-size: 0.9rem;">                当前玩家: <span id="currentPlayer" style="color: #667eea; font-weight: 600;"></span>            </div>        </div>        <!-- 实时排行榜 -->        <div id="leaderboard">            <h2> 实时排行榜 (TOP 10)</h2>            <ul id="leaderboardList"></ul>        </div>        <button id="startBtn">开始游戏</button>        <canvas id="gameCanvas" width="880" height="560"></canvas>        <div class="game-tip">提示：使用方向键控制挡板，空格键快速开始，击破所有砖块获胜！</div>    </div>    <script>        // -------------------------- Firebase 配置 (已配置) --------------------------        const firebaseConfig = {            apiKey: "AIzaSyDBnoXXoKB-Yi2nx2MLEOEKbf4J2F_d0hM",            authDomain: "my-brick-breaker-game.firebaseapp.com",            databaseURL: "https://my-brick-breaker-game-default-rtdb.asia-southeast1.firebasedatabase.app",            projectId: "my-brick-breaker-game",            storageBucket: "my-brick-breaker-game.firebasestorage.app",            messagingSenderId: "957093806478",            appId: "1:957093806478:web:0133a8e5691b306f90a4df",            measurementId: "G-6NVMYCS60P"        };        // 初始化 Firebase        firebase.initializeApp(firebaseConfig);        const database = firebase.database();        const scoresRef = database.ref('brickbreaker_scores');        // -------------------------- 游戏核心变量 --------------------------        let currentUser = '';        let score = 0;        let lives = 3;        let gameRunning = false;        // DOM 元素        const loginScreen = document.getElementById('loginScreen');        const gameScreen = document.getElementById('gameScreen');        const usernameInput = document.getElementById('usernameInput');        const loginBtn = document.getElementById('loginBtn');        const currentPlayerElement = document.getElementById('currentPlayer');        const scoreElement = document.getElementById('score');        const livesElement = document.getElementById('lives');        const startBtn = document.getElementById('startBtn');        const canvas = document.getElementById('gameCanvas');        const ctx = canvas.getContext('2d');        const leaderboardList = document.getElementById('leaderboardList');        // 小球属性        const ball = {            x: canvas.width / 2,            y: canvas.height - 50,            radius: 8,            dx: 4,            dy: -4,            baseSpeed: 4        };        // 挡板属性        const paddle = {            x: canvas.width / 2 - 80,            y: canvas.height - 25,            width: 160,            height: 20,            speed: 12,            dx: 0        };        // 砖块配置        const brickConfig = {            rowCount: 6,            columnCount: 11,            width: 72,            height: 24,            padding: 8,            offsetTop: 70,            offsetLeft: 28        };        let bricks = [];        // -------------------------- 登录逻辑 --------------------------        loginBtn.addEventListener('click', handleLogin);        usernameInput.addEventListener('keydown', (e) => {            if (e.key === 'Enter') handleLogin();        });        function handleLogin() {            const username = usernameInput.value.trim();            if (!username) {                alert('请输入您的游戏昵称！');                return;            }            currentUser = username;            currentPlayerElement.textContent = currentUser;            // 切换界面            loginScreen.style.display = 'none';            gameScreen.style.display = 'flex';            // 初始化游戏和排行榜            initBricks();            setupLeaderboardListener();        }        // -------------------------- 排行榜逻辑 --------------------------        function setupLeaderboardListener() {            // 监听分数变化，实时更新排行榜            scoresRef.orderByChild('score').limitToLast(10).on('value', (snapshot) => {                const scores = [];                snapshot.forEach(childSnapshot => {                    scores.push(childSnapshot.val());                });                // 按分数降序排序                scores.sort((a, b) => b.score - a.score);                // 渲染排行榜                renderLeaderboard(scores);            });        }        function renderLeaderboard(scores) {            leaderboardList.innerHTML = '';            scores.forEach((player, index) => {                const li = document.createElement('li');                li.className = 'leaderboard-item';                // 修正：使用标准的模板字符串插值                li.innerHTML = `                    <span class="rank-number">#${index + 1}</span>                    <span class="player-name">${player.username}</span>                    <span class="player-score">${player.score}</span>                `;                leaderboardList.appendChild(li);            });            // 如果没有数据显示提示            if (scores.length === 0) {                leaderboardList.innerHTML = '<li style="text-align: center; color: rgba(255,255,255,0.5);">暂无玩家数据</li>';            }        }        function submitScore(finalScore) {            // 提交分数到 Firebase            scoresRef.push({                username: currentUser,                score: finalScore,                timestamp: firebase.database.ServerValue.TIMESTAMP            });        }        // -------------------------- 游戏逻辑 --------------------------        // 初始化砖块矩阵        function initBricks() {            bricks = [];            const hueSteps = 360 / brickConfig.rowCount;            for (let col = 0; col < brickConfig.columnCount; col++) {                bricks[col] = [];                for (let row = 0; row < brickConfig.rowCount; row++) {                    const x = col * (brickConfig.width + brickConfig.padding) + brickConfig.offsetLeft;                    const y = row * (brickConfig.height + brickConfig.padding) + brickConfig.offsetTop;                    const hue = row * hueSteps;                    const color = `hsl(${hue}, 75%, 55%)`;                    bricks[col][row] = { x, y, status: 1, color };                }            }        }        // 绘制小球        function drawBall() {            ctx.beginPath();            ctx.arc(ball.x, ball.y, ball.radius, 0, Math.PI * 2);            const gradient = ctx.createRadialGradient(ball.x, ball.y, 0, ball.x, ball.y, ball.radius);            gradient.addColorStop(0, '#ffffff');            gradient.addColorStop(1, '#667eea');            ctx.fillStyle = gradient;            ctx.shadowBlur = 15;            ctx.shadowColor = '#667eea';            ctx.fill();            ctx.closePath();            ctx.shadowBlur = 0;        }        // 绘制挡板        function drawPaddle() {            ctx.beginPath();            ctx.roundRect(paddle.x, paddle.y, paddle.width, paddle.height, 10);            const gradient = ctx.createLinearGradient(paddle.x, paddle.y, paddle.x + paddle.width, paddle.y);            gradient.addColorStop(0, '#667eea');            gradient.addColorStop(1, '#764ba2');            ctx.fillStyle = gradient;            ctx.fill();            ctx.closePath();        }        // 绘制砖块        function drawBricks() {            bricks.forEach(column => {                column.forEach(brick => {                    if (brick.status === 1) {                        ctx.beginPath();                        ctx.roundRect(brick.x, brick.y, brickConfig.width, brickConfig.height, 6);                        ctx.fillStyle = brick.color;                        ctx.fill();                        ctx.strokeStyle = 'rgba(255, 255, 255, 0.2)';                        ctx.lineWidth = 1;                        ctx.stroke();                        ctx.closePath();                    }                });            });        }        // 碰撞检测逻辑        function detectCollisions() {            // 小球与砖块碰撞检测            bricks.forEach(column => {                column.forEach(brick => {                    if (brick.status === 1) {                        if (ball.x > brick.x && ball.x < brick.x + brickConfig.width &&                            ball.y > brick.y && ball.y < brick.y + brickConfig.height) {                            ball.dy = -ball.dy;                            brick.status = 0;                            score += 10;                            updateScore();                            checkWinCondition();                        }                    }                });            });            // 小球与边界碰撞检测            if (ball.x + ball.radius > canvas.width || ball.x - ball.radius < 0) {                ball.dx = -ball.dx;            }            if (ball.y - ball.radius < 0) {                ball.dy = -ball.dy;            }            // 小球与挡板碰撞检测（带角度变化）            if (ball.x > paddle.x && ball.x < paddle.x + paddle.width &&                ball.y + ball.radius > paddle.y) {                const hitPosition = (ball.x - paddle.x) / paddle.width - 0.5;                ball.dx = hitPosition * 10;                // 确保速度矢量保持大致恒定                ball.dy = -Math.sqrt(Math.pow(ball.baseSpeed * 2, 2) - Math.pow(ball.dx, 2));            }            // 小球掉落处理            if (ball.y + ball.radius > canvas.height) {                lives--;                updateLives();                lives <= 0 ? gameOver() : resetBallPosition();            }        }        // 更新得分显示        function updateScore() {            scoreElement.textContent = score;        }        // 更新生命显示        function updateLives() {            livesElement.textContent = lives;        }        // 重置小球位置        function resetBallPosition() {            ball.x = canvas.width / 2;            ball.y = canvas.height - 50;            // 初始速度方向随机化            ball.dx = ball.baseSpeed * (Math.random() > 0.5 ? 1 : -1);            ball.dy = -ball.baseSpeed;            gameRunning = false;            startBtn.style.display = 'block';        }        // 检查胜利条件        function checkWinCondition() {            const allBricksDestroyed = bricks.every(column =>                column.every(brick => brick.status === 0)            );            if (allBricksDestroyed) {                submitScore(score);                setTimeout(() => {                    alert(`恭喜 ${currentUser} 通关！最终得分: ${score}\n您的成绩已上榜！`);                    resetGame();                }, 100);            }        }        // 游戏结束处理        function gameOver() {            submitScore(score);            setTimeout(() => {                alert(`游戏结束！${currentUser} 最终得分: ${score}\n您的成绩已上榜！`);                resetGame();            }, 100);        }        // 重置游戏        function resetGame() {            score = 0;            lives = 3;            updateScore();            updateLives();            initBricks();            resetBallPosition();        }        // 移动挡板        function movePaddle() {            paddle.x += paddle.dx;            paddle.x = Math.max(0, Math.min(canvas.width - paddle.width, paddle.x));        }        // 键盘事件监听        function handleKeyDown(e) {            switch(e.key) {                case 'ArrowRight':                    paddle.dx = paddle.speed;                    break;                case 'ArrowLeft':                    paddle.dx = -paddle.speed;                    break;                case ' ':                    e.preventDefault(); // 防止空格键滚动页面                    if (!gameRunning && currentUser) startGame();                    break;            }        }        function handleKeyUp(e) {            if (e.key === 'ArrowRight' || e.key === 'ArrowLeft') {                paddle.dx = 0;            }        }        // 开始游戏        function startGame() {            gameRunning = true;            startBtn.style.display = 'none';        }        // 游戏主循环        function gameLoop() {            ctx.clearRect(0, 0, canvas.width, canvas.height);            drawBricks();            drawPaddle();            drawBall();            if (gameRunning) {                detectCollisions();                ball.x += ball.dx;                ball.y += ball.dy;            }            movePaddle();            requestAnimationFrame(gameLoop);        }        // 绑定事件        startBtn.addEventListener('click', startGame);        document.addEventListener('keydown', handleKeyDown);        document.addEventListener('keyup', handleKeyUp);        // 启动游戏循环        gameLoop();    </script></body></html>